rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /menu_food/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /menu_drinks/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
